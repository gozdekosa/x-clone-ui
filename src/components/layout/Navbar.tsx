"use client";

const Navbar = () => {
  return (
    <nav className="p-4 flex items-center justify-between border-b sticky top-0 z-10 bg-red-500">
        navbar
    </nav>
    );
}

export default Navbar;